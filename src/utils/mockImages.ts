const baseUrl = 'https://image.xiaomo.info/mock';
export const mockImgCover = (index: number): string => `${baseUrl}/cover/cover_${index}.jpg`;
export const mockImgProduct = (index: number): string => `${baseUrl}/product/product_${index}.jpg`;
export const mockImgAvatar = (index: number): string => `${baseUrl}/avatar/avatar_${index}.jpg`;
