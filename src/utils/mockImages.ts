const baseUrl = '/src/assets/images/mock-images';
export const mockImgCover = (index: number): string => `${baseUrl}/covers/cover_${index}.jpg`;
export const mockImgProduct = (index: number): string => `${baseUrl}/products/product_${index}.jpg`;
export const mockImgAvatar = (index: number): string => `${baseUrl}/avatars/avatar_${index}.jpg`;
